## 1. 判定模型

- [x] 1.1 新增 `INTACT` 判定：無比對命中，但斷詞後為單一完整詞元
- [x] 1.2 `PARTIAL` 語義收斂為「有命中但未蓋滿樣本」
- [x] 1.3 `NONE` 語義收斂為「無命中且斷詞後碎裂」
- [x] 1.4 `PASSING = (FULL, INTACT)`；`CoverageReport.intact_only` 供提示用

## 2. 樣式修正

- [x] 2.1 `op no` 補上前置 `\b`——缺了它會從中間咬進去，產出不存在的識別碼
- [x] 2.2 `op no` 位數放寬至 6（規格寫 3~4 碼，真實樣本有 5 碼）
- [x] 2.3 移除 `tech` 樣式，並在原處記錄移除的理由
- [x] 2.4 `INDEX_FORMAT_VERSION` 5 → 6

## 3. CLI

- [x] 3.1 四種判定各自的標籤與顏色
- [x] 3.2 「完整詞元」單獨提示，說明它為何通過、以及唯一的代價
- [x] 3.3 只有「咬錯」或「碎裂」才回非零狀態碼

## 4. 測試

- [x] 4.1 `INTACT` 的判定與 `ok == True`
- [x] 4.2 「咬錯」比「碎裂」更糟：以舊樣式重現 `1` + `ID:0234.000`
- [x] 4.3 5 碼作業序號被完整圈出
- [x] 4.4 tech 無樣式但詞元完整，且模式 A 仍會切碎它

## 5. 文件與範例

- [x] 5.1 `docs/indexing.md` 的判定表改為四種，並說明兩者的後果差異
- [x] 5.2 `identifier-samples.example.yaml` 補上新學到的形狀（**編造值**）
