# 複製成 `.env`（已 gitignore）後填入。金鑰**不可**寫進 config，設定檔會進版控。
#
# 開發期：OpenRouter
OPENROUTER_API_KEY=sk-or-v1-...
#
# production：LiteLLM 代理的 self-host vLLM。
# 換供應商只需要改 config 的 embedding.base_url / model 與這裡的變數名稱，
# 程式不認得任何一家供應商。
# LITELLM_API_KEY=...
#
# Qdrant server（若有）。留空即用本機檔案模式，不必架服務。
# QDRANT_API_KEY=...
